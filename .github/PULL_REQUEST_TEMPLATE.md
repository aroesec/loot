## What this changes

<!-- If it changes a number a user sees, say which number and why it was wrong before. -->

## Why

## Checklist

- [ ] `pnpm test`, `pnpm typecheck` and `pnpm build` pass
- [ ] Added a test for the bug, with the failure described in the comment
- [ ] Read the invariants in CONTRIBUTING.md, and this change does not break one
- [ ] Taxonomy change? Noted that `db:seed`, `db:reclassify` and
      `db:reconcile-debt` need running
- [ ] No real statement data, account numbers or credentials in the diff
