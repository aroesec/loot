/**
 * Shared with client components, so this file must stay free of any database
 * or server-only import.
 */

/** Classifications below this confidence are surfaced for review. */
export const REVIEW_THRESHOLD = 0.65;
